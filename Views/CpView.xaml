<UserControl
    x:Class="AINovel.Views.CpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,15"
            FontSize="24"
            FontWeight="Bold"
            Foreground="#1B95D9"
            Text="CP管理" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  CP列表  -->
            <Border
                Grid.Column="0"
                Margin="0,0,20,0"
                Padding="20"
                Background="White"
                CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,15"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="#333"
                        Text="CP列表" />

                    <StackPanel
                        Grid.Row="1"
                        Margin="0,0,0,15"
                        Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            Foreground="#666"
                            Text="选择账号:" />
                        <ComboBox
                            Width="200"
                            DisplayMemberPath="AccountName"
                            ItemsSource="{Binding Accounts}"
                            SelectedItem="{Binding SelectedAccount}" />
                        <Button
                            Margin="20,0,10,0"
                            Command="{Binding AddCpCommand}"
                            Content="新增"
                            Style="{StaticResource ButtonPrimary}" />
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding EditCpCommand}"
                            Content="编辑"
                            Style="{StaticResource ButtonDefault}" />
                        <Button
                            Command="{Binding DeleteCpCommand}"
                            Content="删除"
                            Style="{StaticResource ButtonDanger}" />
                    </StackPanel>

                    <DataGrid
                        Grid.Row="2"
                        AutoGenerateColumns="False"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        IsReadOnly="True"
                        ItemsSource="{Binding Cps}"
                        SelectedItem="{Binding SelectedCp}"
                        MouseDoubleClick="DataGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="60"
                                Binding="{Binding Id}"
                                Header="ID" />
                            <DataGridTextColumn
                                Width="150"
                                Binding="{Binding Name}"
                                Header="CP名称" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Description}"
                                Header="简介" />
                            <DataGridTextColumn
                                Width="80"
                                Binding="{Binding PromptId}"
                                Header="提示词ID" />
                            <DataGridTextColumn
                                Width="120"
                                Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd}"
                                Header="创建时间" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!--  编辑面板  -->
            <Border
                Grid.Column="1"
                Width="400"
                Padding="20"
                Background="White"
                CornerRadius="8"
                Visibility="{Binding IsEditing, Converter={StaticResource Boolean2VisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="15"
                        Opacity="0.15"
                        ShadowDepth="3" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#1B95D9"
                        Text="编辑CP" />

                    <TextBlock
                        Margin="0,0,0,5"
                        Foreground="#666"
                        Text="CP名称 *" />
                    <TextBox
                        Margin="0,0,0,15"
                        Style="{StaticResource TextBoxExtend}"
                        Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock
                        Margin="0,0,0,5"
                        Foreground="#666"
                        Text="简介" />
                    <TextBox
                        Height="50"
                        Margin="0,0,0,15"
                        Padding="5"
                        VerticalContentAlignment="Top"
                        AcceptsReturn="True"
                        Style="{StaticResource TextBoxExtend}"
                        Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                        VerticalScrollBarVisibility="Auto" />

                    <TextBlock
                        Margin="0,0,0,5"
                        Foreground="#666"
                        Text="提示词内容" />
                    <TextBox
                        Height="350"
                        Margin="0,0,0,5"
                        Padding="5"
                        VerticalContentAlignment="Top"
                        AcceptsReturn="True"
                        Style="{StaticResource TextBoxExtend}"
                        Text="{Binding EditPromptContent, UpdateSourceTrigger=PropertyChanged}"
                        VerticalScrollBarVisibility="Auto" />
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="11"
                        Foreground="#999"
                        Text="提示词中应包含 &lt;核心梗内容&gt; 占位符"
                        TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal">
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding SaveCpCommand}"
                            Content="保存"
                            Style="{StaticResource ButtonPrimary}" />
                        <Button
                            Command="{Binding CancelEditCommand}"
                            Content="取消"
                            Style="{StaticResource ButtonDefault}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!--  状态提示  -->
        <Border
            Grid.Row="1"
            Margin="0,15,20,0"
            Padding="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="#E3F2FD"
            CornerRadius="4"
            Visibility="{Binding StatusMessage, Converter={StaticResource String2VisibilityConverter}}">
            <TextBlock Foreground="#1976D2" Text="{Binding StatusMessage}" />
        </Border>
    </Grid>
</UserControl>